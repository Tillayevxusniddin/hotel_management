<!DOCTYPE html>
<html lang="uz">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bo‘sh xonalar</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h1>Bo‘sh xonalar</h1>
        <form action="/reservations/filter/available-rooms" method="GET">
            <label for="start_date">Boshlanish sanasi:</label>
            <input type="date" id="start_date" name="start_date" value="<%= start_date || '' %>" required>
            <label for="end_date">Tugash sanasi:</label>
            <input type="date" id="end_date" name="end_date" value="<%= end_date || '' %>" required>
            <button type="submit">Ko‘rish</button>
        </form>
        <% if (error) { %>
            <p style="color: red;"><%= error %></p>
        <% } %>
        <% if (rooms && rooms.length > 0) { %>
            <h2>Natijalar</h2>
            <table>
                <thead>
                    <tr>
                        <th>Xona raqami</th>
                        <th>Turi</th>
                    </tr>
                </thead>
                <tbody>
                    <% rooms.forEach(room => { %>
                        <tr>
                            <td><%= room.room_number %></td>
                            <td><%= room.type_name %></td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        <% } else if (start_date && end_date) { %>
            <p>Bo‘sh xonalar topilmadi</p>
        <% } %>
        <a href="/reservations" class="button">Bandlovlar ro‘yxatiga qaytish</a>
    </div>
</body>
</html>