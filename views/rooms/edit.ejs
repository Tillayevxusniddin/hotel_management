<!DOCTYPE html>
<html lang="uz">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Xonani tahrirlash</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h1>Xonani tahrirlash</h1>
        <form action="/rooms/<%= room.id %>/update" method="POST">
            <div>
                <label for="room_number">Xona raqami:</label>
                <input type="text" id="room_number" name="room_number" value="<%= room.room_number %>" required>
            </div>
            <div>
                <label for="room_type_id">Xona turi:</label>
                <select id="room_type_id" name="room_type_id" required>
                    <% if (roomTypes && roomTypes.length > 0) { %>
                        <% roomTypes.forEach(type => { %>
                            <option value="<%= type.id %>" <%= room.room_type_id === type.id ? 'selected' : '' %>><%= type.type_name %></option>
                        <% }); %>
                    <% } else { %>
                        <option value="">Xona turlari topilmadi</option>
                    <% } %>
                </select>
            </div>
            <div>
                <label for="price_per_night">Bir kechalik narxi:</label>
                <input type="number" id="price_per_night" name="price_per_night" step="0.01" value="<%= room.price_per_night %>" required>
            </div>
            <button type="submit">Saqlash</button>
        </form>
        <br>
        <a href="/rooms/<%= room.id %>" class="button">Xona ma'lumotlariga qaytish</a>
        <a href="/rooms" class="button">Xonalar ro'yxatiga qaytish</a>
        <a href="/" class="button">Bosh sahifaga qaytish</a>
    </div>
    <script src="/js/script.js"></script>
</body>
</html>